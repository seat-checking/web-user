import { ReservationStatusDetail } from 'components/reservationStatus/ReservationStatusDetail';
import type { VFC } from 'common/utils/types';

export const ReservationStatusDetailPage: VFC = () => {
  return <ReservationStatusDetail />;
};
